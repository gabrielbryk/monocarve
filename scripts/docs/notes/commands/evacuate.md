The repeatable `--authorize-protected` escape hatch applies only here.
Canonical authorizations and inclusions are recorded in the report and the
immutable manifest provenance, and change its plan identity. Outbound
application dependencies of an included composition root remain ordinary
reported cuts; omitted composition roots retain the default behavior. Tests and
assets remain blocked unless they are beneath an explicitly authorized selected
root. Multiple route and wiring roots may be included in one evacuation by
repeating both `--source` and `--include-composition`.
