## 1. Qualification Foundations

- [x] 1.1 Replace flat workspace enumeration with the shared bounded segment-glob matcher, use it for both discovery and membership coverage, and verify focused Bun and pnpm tests cover exact paths, nested `*`, zero/one/multiple matches, negation, duplicates, unmatched patterns, unsupported syntax, and root escape.
- [x] 1.2 Add a separate versioned assessment executable identity composed from unchanged `CompilerBuildIdentity` plus semantic version and packaging mode; expose `--version --verbose` and build stamps. Verify source/dist/standalone and stale-build cases; prove no rich fields enter canonical plan provenance, same-build dist/standalone plans validate in both directions, and changed behavioral integrity still fails.
- [x] 1.3 Implement stable diagnostics and the qualification outcome matrix: exit 0 for qualified/allowed-empty, exit 2 plus an explicitly degraded bundle for supported unmatched workspace patterns, and exit 1 with no new bundle for fatal conditions. Preserve legacy command exit mappings. Test fatal precedence, missing/unreadable inputs, duplicate package names, unsafe paths, unsupported globs, and package-dependent unavailable fields without parsing prose.
- [x] 1.4 Restrict `--allow-empty` to an existing root genuinely containing no configured production files; verify tsconfig exclusion, inconsistent scanner output, missing roots, replay mismatch, and drift cannot be overridden, and degraded plus allowed-empty exits 2 while recording the override.
- [x] 1.5 Include behavioral compiler identity in ordinary graph cache keys and expose shared qualification diagnostics in `scan`/`config-doctor` without changing legacy report formats beyond additive fields. Verify independent graph facts remain available for degraded discovery while resolution-dependent metrics and package readiness are explicitly unavailable.

## 2. Assessment Snapshot and Summary

- [ ] 2.1 Establish the scanner/resolver and executable-config read-observation or immutable-input boundary before report integration. Define a versioned input inventory covering source and directory membership, consumers/tests, complete TypeScript program/configuration closure, loaded config dependencies, scanner configuration, workspace declarations, package manifests/exports, consulted installed inputs, lockfiles, symlinks, and absence probes. Prove an unbound or changed observed read fails; do not substitute HEAD checks or target-only hashes. Executable TS/ESM config must run only against immutable captured workspace/dependency inputs and fail closed on other filesystem reads.
- [x] 2.2 Add the assessment snapshot and scanner capture entry point, verify inventory before/after scanning, before dependent analysis, and immediately before publication; scope all graph/syntax/workspace/resolution caches to input authority. Test source addition/deletion, consumer-only mutation, tsconfig inheritance change, package-export change, symlink change, unchanged target hashes with changed consumers, and stale cached facts. Verify one live scanner pass and no hidden rescan on drift.
- [x] 2.3 Add `--replay <assessment-bundle-directory>` with strict manifest/artifact/input/config/commit/executable/runtime verification and zero live scans. Reject provenance-free `--graph`, modified bundles, stale source/package inputs, and identity mismatch with stable diagnostics; preserve legacy discovery replay. Record original capture and current replay provenance separately and document matching-source requirements.
- [x] 2.4 Define versioned diagnostic, source/input identity, qualification, and `ArchitectureSummary` records with deterministic serialization; verify all headline graph/candidate metrics and explicit unavailable values. Reject output/input overlap before excluding validated output paths from input-scoped dirty state; verify unchanged bundle replacement does not contaminate input identity.
- [x] 2.5 Route scan and assessment summary rendering through shared normalized records without removing documented scan fields; verify legacy CLI compatibility and additive schema-version tests.

## 3. Evidence Bundle Publication

- [x] 3.1 Implement canonical-destination exclusive ownership, confined staging, generated-only replacement, prior manifest/artifact validation and pre-replacement revalidation, caught-error rollback, and owned cleanup. Test traversal, ancestor/symlink escape, forged prior ownership paths, duplicates, unrelated contents, modified prior files, and two competing writers; the losing writer must not alter the winner's files.
- [x] 3.2 Add durable publication transition/recovery records. Inject exceptions and process termination after staging, after moving the prior bundle, and after publishing before cleanup; prove whole-old/whole-new/absent visibility, byte-identical caught-error restoration, preserved recovery artifacts, and next-invocation `EVIDENCE_RECOVERY_REQUIRED` without stale-lock theft or automatic deletion. Test a recorded phase lagging a completed rename.
- [x] 3.3 Implement deterministic bundle-relative artifact inventory and manifest serialization with canonical analytical arguments, source/input/config/graph/executable/runtime identity, replay provenance, overrides, qualification status, hashes/sizes, and omission recipes. Verify digest recomputation, no self-hash or temporary names, and publication-only controls cannot alter analytical bytes.
- [x] 3.4 Enforce deterministic per-report limits and optional positive-integer `--max-bytes`, including manifest bytes. Keep raw reports/input inventory mandatory. Test invalid limits and oversized required evidence: publish no new bundle, preserve prior evidence, and recommend only real optional-report omissions or a larger budget.

## 4. Assessment Reports and Command

- [x] 4.1 Add the assessment runner that derives layers, hotspots, bounded portfolio, bounded backlog, and the normalized summary directly from one snapshot, and verify every report carries the same baseline identity and deterministic ordering.
- [x] 4.2 Add deterministic Markdown findings rendering from typed reports, including evidence boundary, qualification limitations, domains, cycles, hotspots, candidate classifications, split overview, omissions, and reproducible next commands; verify golden relationships and negative wording checks rather than one unreviewable large snapshot.
- [x] 4.3 Register read-only `assess --app <name> --evidence-dir <path>` with replay, budget, replacement, empty, bounded/full-output, and mutually exclusive file/hotspot selectors. Reject mutation-only flags. Verify qualified/degraded completion changes only the evidence directory and preserves Git state; test documented crash recovery residue separately.

## 5. Batch Declaration Analysis

- [x] 5.1 Separate TypeScript program construction from per-file analysis; reuse one snapshot-bound program for a selected application's batch and reject cross-application targets. Verify program reuse plus target, consumer, configuration, and dependency drift negatives against the full input inventory.
- [x] 5.2 Implement the explicit completeness policy: collect whole-program configuration/options/global/syntactic/semantic diagnostics; errors and unresolved/unsupported analyzer relationships fail with `SPLIT_ANALYSIS_INCOMPLETE`, while warnings/suggestions/messages remain visible. Test normal analyzer return with unresolved target/consumer imports, an unrelated program error, and warning-only success; requested split failure must prevent assessment publication.
- [x] 5.3 Add explicit batch mode for repeated `--file`, `--split-hotspots`, or `--evidence-dir`, requiring `--app`/`--evidence-dir` and rejecting `--out`/positional targets. Add versioned stdout aggregate and atomic detail/aggregate/input/raw/manifest bundle output using shared publication controls. Test duplicate-basename collision safety, deduplication retaining batch mode, partial failure, and missing/conflicting output options.
- [x] 5.4 Preserve legacy single-file/positional stdout JSON shape, human rendering, `--out`, diagnostic-return policy, and exit semantics. Add compatibility assertions for semantic-error reports and prove new envelopes/completeness rules occur only on explicitly selected batch surfaces.
- [x] 5.5 Add mutually exclusive hotspot selection to standalone batches and `assess` using the captured ranking and configured bound; verify deterministic ties, invalid counts, non-source hotspots, cross-application paths, empty selection, and explicit-file conflicts without rescanning.

## 6. Integrated Proof and Documentation

- [x] 6.1 Add a compact synthetic assessment fixture with nested workspace membership, named cycles, shared hotspots, duplicate split basenames, bounded candidate output, and incomplete-qualification variants; verify relationship assertions and deterministic artifact hashes without private workspace data.
- [x] 6.2 Run repeated live assessments and repeated replay assessments with identical authoritative inputs/options; prove byte identity within each mode and equal graph/metric results across modes while retaining distinct provenance. Include unchanged replacement, stale replay/identity, consumer-only drift, fatal/degraded/override precedence, program diagnostic failures, byte budget, concurrent writer, interruption, and unrelated-file negatives.
- [x] 6.3 Document qualification matrix/exit codes, strict replay authority and legacy refusal, supported glob dialect, mandatory raw/input evidence, optional full reports, batch/legacy output signatures, conservative symbol completeness, plan identity isolation, read-only completion and manual interruption recovery boundaries. Verify signatures against generated help and keep all fixtures/examples synthetic.
- [x] 6.4 Run `bun run typecheck`, focused assessment and adapter tests, `bun test`, `bun run quality`, `bun run cli -- --help`, and `openspec validate add-architecture-assessment --strict`; record explicit exit-zero results and leave the worktree clean except for intended implementation and specification changes.
